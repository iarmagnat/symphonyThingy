{% extends 'base.html.twig' %}

{% block title %}Modifier la Salle{% endblock %}

{% block body %}
    <section class="section">
        <h1>Modifier la Salle</h1>

        <div class="edit-form">
            {{ include('salle/_form.html.twig', {'button_label': 'Update'}) }}
        </div>
        <a href="{{ path('salle_index') }}">Retourner à la liste</a>

        {{ include('salle/_delete_form.html.twig') }}
    </section>
{% endblock %}